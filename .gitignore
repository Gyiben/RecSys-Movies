# Dataset is downloadable, not committed
data/

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Runtime artifacts
.poster_cache.json
*.log
.playwright-mcp/

# Note: .eval_cache.json IS committed — it ships the precomputed evaluation so
# the 512 MB server never has to fit all six models at runtime.
